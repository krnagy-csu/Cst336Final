<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Movie Results</title>
    <link rel="stylesheet" href="/styles.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body id = "bodySearch">
  <%-include('navbar.ejs')%>

<div class="customizeDiv">
  <br>
  <h5>Movie Results:</h5>
<% if (movies.length === 0) { %>
    <p>No movies found.</p>
  <% } else { %>
    <% for (let i = 0; i < Math.min(16,movies.length); i++) { %>
      <% let movie = movies[i]; %>
      <div class ="movieSlots">
        <h5 class = "movieTitle"><%= movie.title %></h5>
        <% if (movie.poster_path) { %>
            <a href="/movie/<%= movie.id %>">
          <img class = "movie-poster" src="https://image.tmdb.org/t/p/w200<%= movie.poster_path %>" alt="<%= movie.title %> poster">
        </a>
        <% } else { %>
          <p>No poster available.</p>
        <% } %>
      </div>
      <% } %>
   
  <% } %>

  <br>
</div>
    
</body>
</html>